{
  "comments": [
    {
      "key": {
        "uuid": "45d06f88_0bae411e",
        "filename": "board/fruitpie/usb_pd_config.h",
        "patchSetId": 1
      },
      "lineNbr": 48,
      "author": {
        "id": 1103909
      },
      "writtenOn": "2014-04-29T17:05:54Z",
      "side": 1,
      "message": "This is a little confusing. If you do:\npd_select_polarity(0);\npd_start_tx(1);\nthen it will actually use polarity 0 for the transmission, so the polarity 1 arg to pd_start_tx(), and these other functions is useless.\n\nIt seems like the real change is in pd_select_polarity(). Can we just rely on user calling that function and remove the polarity arg from all the functions: pd_tx_enable(), pd_tx_disable() pd_start_tx(), pd_tx_done()?",
      "revId": "e8c3924a925be099e708e01857246d036acf602a",
      "serverId": "3ce6091f-6c88-37e8-8c75-72f92ae8dfba",
      "unresolved": false
    },
    {
      "key": {
        "uuid": "c5c37fc9_077ab844",
        "filename": "board/fruitpie/usb_pd_config.h",
        "patchSetId": 1
      },
      "lineNbr": 48,
      "author": {
        "id": 1000016
      },
      "writtenOn": "2014-04-29T17:10:48Z",
      "side": 1,
      "message": "the short answer is that depends on the board.\n(as in https://chromium-review.googlesource.com/#/c/195586/5/board/firefly/usb_pd_config.h )\n\nFruitpie has a single TX path on the STM32 side, but that\u0027s not the case for other boards.\nwe can store a copy of the polarity state in the board specific part, but we might end up messing up the synchronization)",
      "parentUuid": "45d06f88_0bae411e",
      "revId": "e8c3924a925be099e708e01857246d036acf602a",
      "serverId": "3ce6091f-6c88-37e8-8c75-72f92ae8dfba",
      "unresolved": false
    }
  ]
}