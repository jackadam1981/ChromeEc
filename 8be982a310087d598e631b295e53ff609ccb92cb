{
  "comments": [
    {
      "unresolved": false,
      "key": {
        "uuid": "fcce9ac7_ed6646a2",
        "filename": "/PATCHSET_LEVEL",
        "patchSetId": 6
      },
      "lineNbr": 0,
      "author": {
        "id": 1483403
      },
      "writtenOn": "2025-10-03T06:37:12Z",
      "side": 1,
      "message": "Coverage impact for [build](https://cr-buildbucket.appspot.com/build/8702070108991013201):\n  * +0.00% 139004/226618 -\u003e 139022/226637 (+18/+19)\n* common/: 17535/22891 -\u003e 17538/22894 (+3/+3)\n* common/cbi_common.c: 15/17 -\u003e 18/20 (+3/+3)\n  * cbi: 6/14 -\u003e 6/17 (+0/+3)\n* zephyr/drivers/usbc/: 2184/2598 -\u003e 2183/2598 (-1/+0)\n* zephyr/drivers/usbc/pdc_rts54xx.c: 908/1119 -\u003e 907/1119 (-1/+0)\n* zephyr/test/drivers/common_cbi/src/: 206/206 -\u003e 222/222 (+16/+16)\n* zephyr/test/drivers/common_cbi/src/test_common_cbi.c: 206/206 -\u003e 222/222 (+16/+16)\n\n  Commits:\n  * [cbi: Add Unified Firmware and Second-source Config (UFSC)](https://chromium-review.googlesource.com/c/chromiumos/platform/ec/+/6974727) (Wai-Hong Tam \u003cwaihong@google.com\u003e)\n",
      "tag": "autogenerated:Zephyr-Code_coverage",
      "revId": "8be982a310087d598e631b295e53ff609ccb92cb",
      "serverId": "3ce6091f-6c88-37e8-8c75-72f92ae8dfba"
    },
    {
      "unresolved": true,
      "key": {
        "uuid": "5985ccae_67054296",
        "filename": "include/ec_commands.h",
        "patchSetId": 6
      },
      "lineNbr": 6668,
      "author": {
        "id": 1002133
      },
      "writtenOn": "2025-10-07T04:44:57Z",
      "side": 1,
      "message": "Shouldn\u0027t we change this now that we decided to model this differently? The new plan as I understand it is only 4 bytes, not 5, and they should be understood as two separate 64-bit values that probably(?) each deserve their own CBI tag (especially because the AP only wants to request one but not the other).\n\nLet\u0027s please fix this up before we\u0027re stuck with something that doesn\u0027t really make sense for who knows how many years.",
      "revId": "8be982a310087d598e631b295e53ff609ccb92cb",
      "serverId": "3ce6091f-6c88-37e8-8c75-72f92ae8dfba"
    },
    {
      "unresolved": true,
      "key": {
        "uuid": "1d6eb3d3_f85f27d2",
        "filename": "include/ec_commands.h",
        "patchSetId": 6
      },
      "lineNbr": 6668,
      "author": {
        "id": 1000407
      },
      "writtenOn": "2025-10-07T16:50:39Z",
      "side": 1,
      "message": "First, the existing `fw_config` tag for the host command supports only 1 DWORD. Therefore, the EC must use a new tag for transfers larger than 1 DWORD. This new tag, `UFSC`, is 5 DWORDs in size, which is consistent with other infrastructure, like Boxster, the probing tool, the BOM conversion, etc. Since our previous discussion focused only on coreboot, I don\u0027t remember we discussed anything outside this scope, e.g. changing the data size from 5 DWORDs to 4 DWORDs.\n\nRegarding this struct, coreboot can use a single host command to read the new UFSC tag. As the size can be specified in a parameter of the host command. It can either:\n (a) read the entire 5 DWORDs by setting the size to `sizeof(struct cbi_ufsc)` and then cast it to `uint64_t` to pass to the existing backend, or\n (b) read only 2 DWORDs that coreboot cares by setting the size to `sizeof(uint64_t)`.\n\nThis process is more simple than the current implementation, which uses one host command to read the `fw_config` tag (1 DWORD) and another to read the `SSFC` tag (1 DWORD), then combines them into 2 DWORDs to pass to the backend.\n\nIf you prefer the option (b) above, I can move this `struct cbi_ufsc` from this shared `ec_command.h` to some EC-only header file, so that coreboot can use its own type for it.",
      "parentUuid": "5985ccae_67054296",
      "revId": "8be982a310087d598e631b295e53ff609ccb92cb",
      "serverId": "3ce6091f-6c88-37e8-8c75-72f92ae8dfba"
    },
    {
      "unresolved": true,
      "key": {
        "uuid": "bb215eeb_8c6696a9",
        "filename": "include/ec_commands.h",
        "patchSetId": 6
      },
      "lineNbr": 6668,
      "author": {
        "id": 1000407
      },
      "writtenOn": "2025-10-07T18:30:25Z",
      "side": 1,
      "message": "I have further confirmed that option (b) doesn\u0027t work. The host command requires the size to match the one stored in the CBI. So, only option (a): coreboot casts the response to `uint64_t`.",
      "parentUuid": "1d6eb3d3_f85f27d2",
      "revId": "8be982a310087d598e631b295e53ff609ccb92cb",
      "serverId": "3ce6091f-6c88-37e8-8c75-72f92ae8dfba"
    },
    {
      "unresolved": true,
      "key": {
        "uuid": "be80ef1c_ca205f1a",
        "filename": "include/ec_commands.h",
        "patchSetId": 6
      },
      "lineNbr": 6668,
      "author": {
        "id": 1002133
      },
      "writtenOn": "2025-10-09T01:16:19Z",
      "side": 1,
      "message": "\u003e Since our previous discussion focused only on coreboot, I don\u0027t remember we discussed anything outside this scope, e.g. changing the data size from 5 DWORDs to 4 DWORDs.\n\nWell, the number of DWORDs necessarily gets reduced (from 5 to 4) with the changes we discussed, because the \"OEM\" spaces got included in the 64 bits that coreboot gets and then a separate OEM DWORD doesn\u0027t make sense anymore. I just sent another email to Aamir to clarify that, but I think there\u0027s no other logical option with the changes we discussed. (I assume that they\u0027re going to modify their other infrastructure like Boxster accordingly.)\n\nAlso, from an organizational perspective, I think it makes more sense to consider these two separate `uint64_t` instead of four `uint32_t`. I totally agree with you that we should deprecate the existing `fw_config`/`SSFC` tags and make new ones, I\u0027m just saying that you should make two new tags that contain one `uint64_t` each, rather than this array of `uint32_t` here. Then coreboot can request one of them and doesn\u0027t need to care about the other.",
      "parentUuid": "bb215eeb_8c6696a9",
      "revId": "8be982a310087d598e631b295e53ff609ccb92cb",
      "serverId": "3ce6091f-6c88-37e8-8c75-72f92ae8dfba"
    },
    {
      "unresolved": true,
      "key": {
        "uuid": "a6034018_a84455c7",
        "filename": "include/ec_commands.h",
        "patchSetId": 6
      },
      "lineNbr": 6668,
      "author": {
        "id": 1000407
      },
      "writtenOn": "2025-10-09T16:21:58Z",
      "side": 1,
      "message": "**5 DWORD vs 4 DWORD**: The 64-bit is a limitation of coreboot; otherwise, a massive change would be required. The situation for the EC is different as it has to introduce something new. I\u0027d prefer over-reservation, having a dedicated DWORD for OEM-defined fields. However, it is not a big deal. I would let Aamir to decide.\n\n**`uint64_t` vs `uint32_t`**: Unlike the AP, all ECs supported so far still use 32-bit architecture. Using `uint64_t` has worse performance than `uint32_t`, as the compiler must translate `uint64_t` operations into multiple `uint32_t` instructions. Therefore, `struct cbi_ufsc { uint32_t data[COUNT] }` is used instead of `struct cbi_ufsc { uint64_t data[COUNT] }`.\n\n**1 tag vs 2 tags**: Let\u0027s consider this as a provider-consumer case. The UFSC tag (or 2 tags) stored in CBI is the provider. The EC is one consumer, coreboot is another, the factory provisioning it is another, the build system constructing it is yet another. I understand coreboot only inspect 2 DWORDs (or 1 tag), but all other use cases inspect the entire 5 DWORDs (2 tags). Changing the provider to optimize for a single consumer at the expense of the others seems not a good idea.",
      "parentUuid": "be80ef1c_ca205f1a",
      "revId": "8be982a310087d598e631b295e53ff609ccb92cb",
      "serverId": "3ce6091f-6c88-37e8-8c75-72f92ae8dfba"
    }
  ]
}