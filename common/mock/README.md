# Common Mocks
This directory holds mock implementations for fuzzers and tests.

They can be activated from within the fuzzer/test specific `*.mocklist` file.

The associated header files are in `include/mock` directory.