# Copyright (c) 2021 The Chromium OS Authors
# SPDX-License-Identifier: Apache-2.0

description: The node has the information required by motion sense running

compatible: "cros-ec,motionsense-sensor-info"

properties:
    als-sensors:
        type: phandles
        required: false
        description: list of ALS sensors to create motion_als_sensors array
    sensor-irqs:
        type: phandles
        required: false
        description: list of gpio interrupts from sensors to be enabled.
    accel-force-mode-sensors:
        type: phandles
        required: false
        description: list of sensors should be handled in force mode.
