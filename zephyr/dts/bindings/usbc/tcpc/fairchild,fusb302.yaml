description: USBC TCPC

compatible: "fairchild,fusb302"

include: i2c-device.yaml

properties:
  tcpc-flags:
    type: int
    default: 0
    description: |
      TCPC configuration flags
  irq-gpios:
    type: phandle-array
    required: false
    description: |
      Interrupt from TCPC
