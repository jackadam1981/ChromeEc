 # Copyright 2022 The Chromium OS Authors. All rights reserved.
 # Use of this source code is governed by a BSD-style license that can be
 # found in the LICENSE file.

description: |
  Emulated power signal.

compatible: "ap-pwr-signal-emul"

properties:
  output-signal:
    type: phandle
    required: true
    description: |
      Output power signal that will trigger input signals for
      power sequence.

  assert-value:
    type: int
    required: false
    default: 1

  edge:
    type: string
    required: false
    description: |
      Output signal edge that will activate corresponding input signals.
    default: EDGE_ACTIVE_ON_BOTH
    enum:
      - EDGE_ACTIVE_ON_ASSERT
      - EDGE_ACTIVE_ON_DEASSERT
      - EDGE_ACTIVE_ON_BOTH

  init-value:
    type: int
    required: false

child-binding:
  description:
    Input signal operation that will be triggered by this output signal.

  properties:
    input-signal:
      type: phandle
      required: true

    assert-value:
      type: int
      required: false
      default: 1

    assert-delay-ms:
      type: int
      required: false
      default: 10

    deassert-value:
      type: int
      required: false
      default: 0

    deassert-delay-ms:
      type: int
      required: false
      default: 10

    init-value:
      type: int
      required: false

    invert-value:
      type: boolean
