common:
  platform_allow: native_sim
  tags:
    system
tests:
  system_shim.default:
    extra_dtc_overlay_files:
    - ./default.overlay
  system_shim.ish:
    extra_configs:
    - CONFIG_SYSTEM_SHIM_TEST_ISH=y
  system_shim.powerbtn:
    extra_configs:
    - CONFIG_POWER_BUTTON_INIT_IDLE=y
