{
  "comments": [
    {
      "unresolved": true,
      "key": {
        "uuid": "bb6edc6d_eb605ac8",
        "filename": "zephyr/program/nissa/craask/src/keyboard.c",
        "patchSetId": 1
      },
      "lineNbr": 63,
      "author": {
        "id": 1281040
      },
      "writtenOn": "2022-11-30T10:00:20Z",
      "side": 1,
      "message": "I suggest adding a global static bool that is set in the kb_init() function\nset to indicate a numeric pad is absent or present.\nI think you can fold the 2 functions board_update_no_keypad_by_fwconfig() and\nkb_init together (don\u0027t need to split these). Just have a kb_init() function.\n\nI think this function should also be the only function that calls\ncros_cbi_get_fw_config(), and then set the global static bool.\nThe return from this function should be checked, since it is possible that\nthe fw_config CBI fields are invalid, so in the case that the fw_config is\ninvalid, set the bool to an appropriate value.",
      "revId": "2bfad1f231a28c54a35e8bf1320c8e0f8f540458",
      "serverId": "3ce6091f-6c88-37e8-8c75-72f92ae8dfba"
    }
  ]
}